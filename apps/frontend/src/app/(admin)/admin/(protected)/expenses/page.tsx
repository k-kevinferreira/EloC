import { ModulePlaceholder } from '@/components/admin/module-placeholder';

export default function AdminExpensesPage() {
  return (
    <ModulePlaceholder
      eyebrow="Financas"
      title="Despesas entram no proximo ciclo de backend"
      description="A pagina ja existe dentro da navegacao para consolidar a arquitetura do painel, mas a regra de negocio de expenses continua reservada ao backend antes da implementacao completa do frontend."
    />
  );
}
